@using RestaurantWebBL.Interfaces
@model PV179_RestaurantWeb.Models.LoginViewModel

@inject ILocalizationService LocalizationService
@{
    ViewData["Title"] = @LocalizationService.GetStringWithCode("en", "login").LocalizedString;
}

<h1>@ViewData["Title"]</h1>

<div class="row">
    <div class="col-md-4">

        <section>
            @using (Html.BeginForm())
            {
                @Html.HiddenFor(x => x.ReturnUrl)
                @Html.AntiForgeryToken()
                <hr />
                @Html.ValidationSummary(string.Empty, new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(x => x.UserName, labelText:@LocalizationService.GetStringWithCode("en","name").LocalizedString)
                    @Html.TextBoxFor(x => x.UserName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.UserName, null, new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(x => x.Password, labelText:@LocalizationService.GetStringWithCode("en","password").LocalizedString)
                    @Html.PasswordFor(x => x.Password, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.Password, null, new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary mt-1">@LocalizationService.GetStringWithCode("en","login").LocalizedString</button>
                </div>
            }
        </section>
    </div>
</div>